import { AsyncTop10List } from '@/widgets/AsyncTop10List';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Top 10 Rackets",
    description: "Хиты продаж сайта Tennis store"
}

export default AsyncTop10List